"use client";

import { ProductPage } from "@/components/pages/product-page";

/**
 * 产品开发 — 交互基准：灵犀前端 ProductPage（趋势 / 机会 / 创意）
 * 增强：拉取 GET /market/opportunities 数量提示；8 周趋势与创意推进仍为 mock（GAP）
 */
export default function MarketRoutePage() {
  return <ProductPage />;
}
