[project]
name = "lingxi-data-jobs"
version = "0.1.0"
description = "Lingxi global market data jobs"
requires-python = ">=3.11,<3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/lingxi_data_jobs"]